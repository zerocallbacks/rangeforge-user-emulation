package controller



// ValidateEnvironment confirms the host environment is supported (Windows, Linux, FreeBSD, pfSense, macOS).
func ValidateEnvironment(allowUnsupportedOS bool) error {
	// Full first-class cross-platform support across Windows and Unix
	return nil
}
